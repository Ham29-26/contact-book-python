# Contact Book (Python)

This is a simple command-line contact book built with Python.

### Features:
- Add new contacts
- View all saved contacts
- Search for a contact by name
- Contacts are stored in a local `contacts.txt` file and persist between sessions

### How to Run:
Make sure you have Python installed, then run:
```bash
python contact_book.py
